---
title: (Admin) Sync GitHub
description: >
  Manually triggers a GitHub sync. **Administrators only** — the caller's
  session email must

  match the configured `ADMIN_EMAIL`; an API key cannot reach this route.


  Incremental by default: only issues updated since the last sync are refetched.
  The run is

  guarded by a 10-minute Redis lock, so a concurrent attempt fails with `409`
  rather than

  double-syncing. The response is the updated sync record.
full: true
_openapi:
  preload:
    - ./openapi.yaml
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >
          Manually triggers a GitHub sync. **Administrators only** — the
          caller's session email must

          match the configured `ADMIN_EMAIL`; an API key cannot reach this
          route.


          Incremental by default: only issues updated since the last sync are
          refetched. The run is

          guarded by a 10-minute Redis lock, so a concurrent attempt fails with
          `409` rather than

          double-syncing. The response is the updated sync record.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp
        document="./openapi.yaml"
        operations={[{ path: "/api/v1/github/admin/sync", method: "post" }]}
      />
    </>
  );
}
